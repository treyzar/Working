export enum EInputTypes {
  TEXT = "text",
  PASSWORD = "password",
  EMAIL = "email",
  NUMBER = "number",
  DATE = "date",
  TIME = "time",
  FILE = "file",
  RADIO = "radio",
  CHECKBOX = "checkbox",
  SELECT = "select",
  TEXTAREA = "textarea",
  BUTTON = "button",
  SUBMIT = "submit",
  RESET = "reset",
  IMAGE = "image",
}

export enum EButtonTypes {
  BUTTON = "button",
  SUBMIT = "submit",
  RESET = "reset",
}

export enum EMethods {
  GET = "GET",
  POST = "POST",
  PUT = "PUT",
  DELETE = "DELETE",
  PATCH = "PATCH",
}
